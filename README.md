# Virtual Tour 360 - Docker Setup

## Requirements
- Docker
- Git 

## Setup
git clone https://github.com/ramanawebronic-cyber/virtual-tour-360.git
cd REPO-NAME
cp .env.example .env
docker compose up -d --build

Open http://localhost:8080
